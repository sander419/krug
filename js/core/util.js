// file: js/core/util.js
// Общие мелочи ядра. Отдельный файл, чтобы одна и та же функция не жила
// в четырёх модулях с четырьмя разными опечатками.

/* Зажать значение в границы. */
export const clamp = (v, a, b) => Math.min(b, Math.max(a, v));

/* Плавное затухание 0…1 — для переходов между этапами. */
export const smoothstep = f => f * f * (3 - 2 * f);
